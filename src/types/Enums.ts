export enum OrderColor {
	GREEN = "#00A300",
	RED = "#E74C3C"
}

export enum OrderType {
	ASK = "ask",
	BID = "bid"
}

export enum Product {
	PI_XBTUSD = "PI_XBTUSD",
	PI_ETHUSD = "PI_ETHUSD"
}

export enum WebsocketEventEnum {
	SUBSCRIBE = "subscribe",
	UNSUBSCRIBE = "unsubscribe"
}